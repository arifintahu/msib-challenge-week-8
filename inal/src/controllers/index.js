const movieControllers = require("./movie.controllers");
const logControllers = require("./log.controllers")
// TODO: Add controller logControllers

module.exports = {
  movieControllers,
  logControllers
};
