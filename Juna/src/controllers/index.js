const movieControllers = require("./movie.controllers");
const logControllers = require('./logControllers')

// TODO: Add controller logControllers

module.exports = {
  movieControllers,
  logControllers
};
